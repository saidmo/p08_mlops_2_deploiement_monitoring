# ----------------------------------------------------------------------------
# Dépendances RUNTIME de l'API (ce qui part dans l'image Docker)
# ----------------------------------------------------------------------------
fastapi
uvicorn[standard]
pydantic
python-json-logger

# ----------------------------------------------------------------------------
# Pile numérique — VERSIONS ÉPINGLÉES pour la reproductibilité
# ----------------------------------------------------------------------------
# Ces versions figées garantissent des prédictions IDENTIQUES sur les trois
# environnements : poste Windows, image Docker et poste macOS. Le pickle
# model_credit_scoring.pkl a été entraîné/validé avec exactement ces versions
# (proba de référence obtenue : 0.7765756075). Ne pas modifier sans re-tester
# la non-régression sur l'ensemble du pipeline.
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
scikit-learn==1.8.0
lightgbm==4.6.0
