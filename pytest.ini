[pytest]
# Ajoute la racine du projet au sys.path pour que `from app.main import app`
# et `import features` (déclenché par le chargement du pipeline) fonctionnent
# de la même manière en local et dans la CI GitHub Actions.
pythonpath = .
testpaths = tests
