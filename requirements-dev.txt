# ----------------------------------------------------------------------------
# Dépendances de DEV / TEST / MONITORING
# (utilisées en CI et en local, PAS dans l'image de prod)
# ----------------------------------------------------------------------------
-r requirements.txt

# Tests
pytest
pytest-cov     # taux de couverture (demandé par la fiche d'auto-évaluation)
httpx          # requis par le TestClient de FastAPI, et par Streamlit pour appeler l'API

# Monitoring & data drift
streamlit
evidently
